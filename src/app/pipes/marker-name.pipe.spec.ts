import { MarkerNamePipe } from './marker-name.pipe';

describe('MarkerNamePipe', () => {
  it('create an instance', () => {
    const pipe = new MarkerNamePipe();
    expect(pipe).toBeTruthy();
  });
});
